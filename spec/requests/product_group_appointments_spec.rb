require 'rails_helper'

# This spec was generated by rspec-rails when you ran the scaffold generator.
# It demonstrates how one might use RSpec to test the controller code that
# was generated by Rails when you ran the scaffold generator.
#
# It assumes that the implementation code is generated by the rails scaffold
# generator. If you are using any extension libraries to generate different
# controller code, this generated spec may or may not pass.
#
# It only uses APIs available in rails and/or rspec-rails. There are a number
# of tools you can use to make these specs even more expressive, but we're
# sticking to rails and rspec-rails APIs to keep things simple and stable.

RSpec.describe "/product_group_appointments", type: :request do
  # This should return the minimal set of attributes required to create a valid
  # ProductGroupAppointment. As you add validations to ProductGroupAppointment, be sure to
  # adjust the attributes here as well.
  let(:valid_attributes) {
    skip("Add a hash of attributes valid for your model")
  }

  let(:invalid_attributes) {
    skip("Add a hash of attributes invalid for your model")
  }

  describe "GET /index" do
    it "renders a successful response" do
      ProductGroupAppointment.create! valid_attributes
      get product_group_appointments_url
      expect(response).to be_successful
    end
  end

  describe "GET /show" do
    it "renders a successful response" do
      product_group_appointment = ProductGroupAppointment.create! valid_attributes
      get product_group_appointment_url(product_group_appointment)
      expect(response).to be_successful
    end
  end

  describe "GET /new" do
    it "renders a successful response" do
      get new_product_group_appointment_url
      expect(response).to be_successful
    end
  end

  describe "GET /edit" do
    it "renders a successful response" do
      product_group_appointment = ProductGroupAppointment.create! valid_attributes
      get edit_product_group_appointment_url(product_group_appointment)
      expect(response).to be_successful
    end
  end

  describe "POST /create" do
    context "with valid parameters" do
      it "creates a new ProductGroupAppointment" do
        expect {
          post product_group_appointments_url, params: { product_group_appointment: valid_attributes }
        }.to change(ProductGroupAppointment, :count).by(1)
      end

      it "redirects to the created product_group_appointment" do
        post product_group_appointments_url, params: { product_group_appointment: valid_attributes }
        expect(response).to redirect_to(product_group_appointment_url(ProductGroupAppointment.last))
      end
    end

    context "with invalid parameters" do
      it "does not create a new ProductGroupAppointment" do
        expect {
          post product_group_appointments_url, params: { product_group_appointment: invalid_attributes }
        }.to change(ProductGroupAppointment, :count).by(0)
      end

      it "renders a response with 422 status (i.e. to display the 'new' template)" do
        post product_group_appointments_url, params: { product_group_appointment: invalid_attributes }
        expect(response).to have_http_status(:unprocessable_content)
      end
    end
  end

  describe "PATCH /update" do
    context "with valid parameters" do
      let(:new_attributes) {
        skip("Add a hash of attributes valid for your model")
      }

      it "updates the requested product_group_appointment" do
        product_group_appointment = ProductGroupAppointment.create! valid_attributes
        patch product_group_appointment_url(product_group_appointment), params: { product_group_appointment: new_attributes }
        product_group_appointment.reload
        skip("Add assertions for updated state")
      end

      it "redirects to the product_group_appointment" do
        product_group_appointment = ProductGroupAppointment.create! valid_attributes
        patch product_group_appointment_url(product_group_appointment), params: { product_group_appointment: new_attributes }
        product_group_appointment.reload
        expect(response).to redirect_to(product_group_appointment_url(product_group_appointment))
      end
    end

    context "with invalid parameters" do
      it "renders a response with 422 status (i.e. to display the 'edit' template)" do
        product_group_appointment = ProductGroupAppointment.create! valid_attributes
        patch product_group_appointment_url(product_group_appointment), params: { product_group_appointment: invalid_attributes }
        expect(response).to have_http_status(:unprocessable_content)
      end
    end
  end

  describe "DELETE /destroy" do
    it "destroys the requested product_group_appointment" do
      product_group_appointment = ProductGroupAppointment.create! valid_attributes
      expect {
        delete product_group_appointment_url(product_group_appointment)
      }.to change(ProductGroupAppointment, :count).by(-1)
    end

    it "redirects to the product_group_appointments list" do
      product_group_appointment = ProductGroupAppointment.create! valid_attributes
      delete product_group_appointment_url(product_group_appointment)
      expect(response).to redirect_to(product_group_appointments_url)
    end
  end
end
